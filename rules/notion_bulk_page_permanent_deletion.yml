# schema: https://scanner.dev/schema/scanner-detection-rule.v1.json
name: Notion Bulk Permanent Page Deletion
description: |-
  Detect when more than 10 pages are permanently deleted within a 1-hour
  window. Bulk permanent deletion is destructive and unrecoverable; it may
  indicate an attacker destroying data or a malicious insider.

  Note: Notion's own retention process (actor domain makenotion.com) performs
  routine permanent deletions from Trash and is excluded to reduce noise. If
  you want visibility into those, remove the exclusion.
severity: Low
query_text: |-
  @scnr.source_type:notion
  event.type:page.permanently_deleted
  not event.actor.person.email:*@makenotion.com
  | count()
  | where @q.count > 10
time_range_s: 3600
run_frequency_s: 60
event_sink_keys:
- low_severity_alerts
tags:
- tactics.ta0040.impact
- techniques.t1485.data_destruction
- source.notion
